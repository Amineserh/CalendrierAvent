<UserControl x:Class="CalendrierAvent.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>

        <!-- Style des cases du calendrier -->
        <Style x:Key="DayButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Border"
                                    CornerRadius="14"
                                    BorderThickness="2">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#FF5A0F0F" Offset="0"/>
                                        <GradientStop Color="#FFD32F2F" Offset="0.35"/>
                                        <GradientStop Color="#FFFFC107" Offset="0.7"/>
                                        <GradientStop Color="#FF1B5E20" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#FFFFF8E1" Offset="0"/>
                                        <GradientStop Color="#FFFFD740" Offset="0.5"/>
                                        <GradientStop Color="#FFFFF8E1" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                                <Border.Effect>
                                    <DropShadowEffect Color="#FFFFF9C4"
                                                      BlurRadius="18"
                                                      ShadowDepth="0"
                                                      Opacity="0.75"/>
                                </Border.Effect>
                                <Border.RenderTransform>
                                    <ScaleTransform x:Name="BorderScale"
                                                    ScaleX="1"
                                                    ScaleY="1"/>
                                </Border.RenderTransform>
                            </Border>

                            <TextBlock Text="{TemplateBinding Content}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="{TemplateBinding FontSize}"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       Foreground="{TemplateBinding Foreground}">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="Black"
                                                      BlurRadius="8"
                                                      ShadowDepth="0"
                                                      Opacity="0.9"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderThickness" Value="3"/>
                                <Setter TargetName="Border" Property="BorderBrush">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FFFFF59D" Offset="0"/>
                                            <GradientStop Color="#FFFFEB3B" Offset="0.5"/>
                                            <GradientStop Color="#FFFFF59D" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="BorderScale"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="1.06"
                                                             Duration="0:0:0.08" />
                                            <DoubleAnimation Storyboard.TargetName="BorderScale"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="1.06"
                                                             Duration="0:0:0.08" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="BorderScale"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="1"
                                                             Duration="0:0:0.08" />
                                            <DoubleAnimation Storyboard.TargetName="BorderScale"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="1"
                                                             Duration="0:0:0.08" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <!-- Désactivé = jour futur -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Anim apparition calendrier -->
        <Storyboard x:Key="CalendarAppearStoryboard">
            <DoubleAnimation Storyboard.TargetName="CalendarOverlay"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1"
                             Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="CalendarScale"
                             Storyboard.TargetProperty="ScaleX"
                             From="0.6" To="1"
                             Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="CalendarScale"
                             Storyboard.TargetProperty="ScaleY"
                             From="0.6" To="1"
                             Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

    </UserControl.Resources>

    <Grid Background="Black">

        <!-- Vidéo intérieur.mp4 -->
        <MediaElement x:Name="NextVideo"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Stretch="UniformToFill"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Stop"/>

        <!-- Texte au cas où vidéo manque -->
        <TextBlock x:Name="NextPageText"
                   Text="Nouvelle page en préparation..."
                   Foreground="White"
                   FontSize="32"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextAlignment="Center"
                   Opacity="0.7" />

        <!-- Zone sapin, invisible -->
        <Button x:Name="TreeButton"
                Width="220"
                Height="500"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="320,0,0,0"
                Background="Transparent"
                BorderThickness="0"
                Opacity="0"
                Focusable="False"
                Click="TreeButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- Overlay calendrier -->
        <Grid x:Name="CalendarOverlay"
              Background="#80000000"
              Visibility="Collapsed"
              Opacity="0">
            <Border x:Name="CalendarContainer"
                    Background="#FF1E1E1E"
                    CornerRadius="12"
                    Padding="20"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <ScaleTransform x:Name="CalendarScale" ScaleX="0.6" ScaleY="0.6"/>
                </Border.RenderTransform>

                <StackPanel>
                    <TextBlock Text="Calendrier de l'Avent"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="#FFFFD54F"
                               TextAlignment="Center"
                               Margin="0,0,0,10" />

                    <UniformGrid x:Name="CalendarGrid"
                                 Rows="4" Columns="6"
                                 Margin="10">
                        <!-- 24 cases -->
                        <Button Content="1"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="2"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="3"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="4"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="5"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="6"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="7"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="8"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="9"  Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="10" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="11" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="12" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="13" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="14" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="15" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="16" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="17" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="18" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="19" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="20" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="21" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="22" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="23" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                        <Button Content="24" Style="{StaticResource DayButtonStyle}" Click="DayButton_Click"/>
                    </UniformGrid>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Popup cadeau -->
        <Grid x:Name="GiftPopup"
              Background="#CC000000"
              Visibility="Collapsed">
            <Border Background="#FF2C2C2C"
                    CornerRadius="16"
                    Padding="20"
                    Width="600"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="GiftTitleText"
                               Text="Cadeau du jour"
                               FontSize="26"
                               FontWeight="Bold"
                               Foreground="#FFFFD54F"
                               TextAlignment="Center"
                               Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="1">
                        <Image x:Name="GiftImage"
                               Height="260"
                               Stretch="Uniform"
                               Margin="0,0,0,10"/>
                        <TextBlock x:Name="GiftMessageText"
                                   Text=""
                                   FontSize="18"
                                   Foreground="White"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="10,0,10,0"/>
                    </StackPanel>

                    <Button Grid.Row="2"
                            Content="Fermer"
                            Width="120"
                            Height="38"
                            HorizontalAlignment="Center"
                            Margin="0,15,0,0"
                            Background="#CC1A1A"
                            Foreground="White"
                            FontWeight="Bold"
                            BorderThickness="0"
                            Click="CloseGiftPopupButton_Click"/>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</UserControl>
